export interface PokemonData {
  user_id: string;
  area_id: number;
  email: string;
}

export interface PokemonGenData {
  user_id: string;
  area_id: number;
  email: string;
  generation: number;
}
